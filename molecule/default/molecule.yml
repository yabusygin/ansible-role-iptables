---
dependency:
  name: galaxy
driver:
  name: vagrant
  provider:
    name: virtualbox
lint:
  name: yamllint
platforms:
  - name: gateway
    box: debian/stretch64
  - name: inside-host
    box: debian/stretch64
  - name: dmz-host
    box: debian/stretch64
  - name: outside-host
    box: debian/stretch64
provisioner:
  name: ansible
  lint:
    name: ansible-lint
verifier:
  name: ansible
  lint:
    name: ansible-lint
